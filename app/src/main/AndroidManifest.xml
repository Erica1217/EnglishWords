<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a10105_.english_words">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Game_english"/>
        <activity android:name=".Game_hanguel"/>
        <activity android:name=".Game_menu"/>
        <activity android:name=".Menu"/>
        <activity android:name=".Word_add"/>
        <activity android:name=".Word_delete"/>
        <activity android:name=".Word_search"/>
        <activity android:name=".Result"/>

    </application>

</manifest>